```mermaid
sequenceDiagram
    participant browser
    participant server

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of browser: status code: 201 created, -><br> {"content": "0_6 understood","date": "2024-07-16T16:52:49.543Z"}
    server-->>browser: application/json
    deactivate server

    Note right of browser: The browser starts executing the JavaScript code that fetches the JSON from the server -><br> (The browser executes the callback function that renders the notes)
```
